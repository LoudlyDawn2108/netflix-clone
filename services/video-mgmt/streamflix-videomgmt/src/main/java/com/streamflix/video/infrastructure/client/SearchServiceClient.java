package com.streamflix.video.infrastructure.client;

// Define methods for interacting with the Search & Discovery Service
// e.g., indexDocument(VideoDocument videoDocument), deleteDocument(String videoId)
public interface SearchServiceClient {
    // TODO: Define methods based on actual Search & Discovery Service API
    // This client might not be strictly necessary if all communication
    // to Search Service is via Kafka events as per README.
    // However, it can be useful for direct calls if any are needed.
}
